application.version: TESTING

spinestack:
  database:
    file: ":memory:"

spring:
  flyway:
    enabled: true
  artemis:
    embedded:
      persistent: false

logging:
  level:
    io.github.mscodedev.spinestack: DEBUG
